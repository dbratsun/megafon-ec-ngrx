/*
import { TestBed, inject } from '@angular/core/testing';

import { PlacemarkManager } from './placemark-manager';

describe('PlacemarkManager', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [PlacemarkManager]
    });
  });

  it('should be created', inject([PlacemarkManager], (service: PlacemarkManager) => {
    expect(service).toBeTruthy();
  }));
});
*/